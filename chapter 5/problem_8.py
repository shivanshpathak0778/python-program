#can u change the inside a list which is contained in a set
s={8,9,10, "shivam", [1, 2, 3]}
# it will give an error because list is mutable
#  and set is immutable so we can not change the inside of a set